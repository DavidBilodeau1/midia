{% extends 'base.html.twig' %}
{% block content %}
<link rel="stylesheet" href="{{asset('css/book.css')}}" />
<nav class="mt-10 ml-20">
  <ul class="breadcrumb">
    <li class="breadcrumb-item"><a href="{{path('home')}}">Accueil</a></li>
    <li class="breadcrumb-item"><a href="{{path('books')}}">Séries de livres</a></li>
    <li class="breadcrumb-item active" aria-current="page"><a href="{{ path('book', {book_id : book.id }) }}">{{ book.name }}</a></li>
  </ul>
</nav>
<input type="range" min="1" max="100" value="50" class="slider" id="myRange">
<div class="container-fluid h-full">
<div id="zoom-viewport" class="container-fluid position-absolute overflow-scroll centered mt-20">
    <div id="flipbook" class="m-auto font-size-14 w-1080 h-800" style="line-height: 20px;">
                    
    </div>
    </div>
    <div class="progress-group m-auto w-half bottom-centered">
        <div class="progress">
            <div class="progress-bar progress-bar-animated" role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
        </div>
        <span class="progress-group-label">0 / 0</span>
    </div>
</div>
{% endblock %}
{% block javascripts %}
    {{parent()}}
        <script type="text/javascript" src="{{asset('js/book.js')}}" ></script>
        <script type="text/javascript">

        </script>
{% endblock %}
